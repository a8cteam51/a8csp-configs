<?xml version="1.0"?>
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../../vendor/squizlabs/php_codesniffer/phpcs.xsd">
	<!-- The rules shared with the tests profile, floors included. -->
	<rule ref="./phpcs.base.dist.xml"/>

	<!-- Test code is linted against a dedicated companion profile (phpcs.tests.dist.xml) — a
	     distinct profile natively expresses per-context rules and property values, which a
	     per-sniff scoped exclude-pattern cannot. -->
	<exclude-pattern>*/tests/*</exclude-pattern>

	<!-- Disallow blank lines at start/end of for/foreach/if blocks. -->
	<rule ref="WordPress.WhiteSpace.ControlStructureSpacing">
		<properties>
			<property name="blank_line_check" value="true"/>
		</properties>
	</rule>
</ruleset>
