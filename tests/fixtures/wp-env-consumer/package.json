{
  "name": "a8csp-wp-env-consumer-fixture",
  "version": "0.0.0",
  "private": true,
  "description": "Declares the wp-env that reusable-phpunit.yml starts for this fixture, the way a real consumer does.",
  "license": "MIT",
  "devDependencies": {
    "@wordpress/env": "^11.14.0"
  }
}
